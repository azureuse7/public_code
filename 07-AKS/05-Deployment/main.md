# Kubernetes Deployment

A Deployment is a Kubernetes object that manages a set of identical pods, ensuring that a specified number of replicas of the pod are running at any given time. Deployments also support rolling updates and rollbacks, making them the preferred way to manage stateless applications.
